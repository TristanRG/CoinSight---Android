<resources>
    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your theme here -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primaryDark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:colorBackground">@color/white</item>
        <!-- Add more theme attributes as needed -->
    </style>
    <style name="WhiteScrollBar">
        <item name="android:scrollbarThumbVertical">@drawable/scrollbar_thumb_vertical_white</item>
        <item name="android:scrollbarTrackVertical">@drawable/scrollbar_track_vertical_white</item>
    </style>
</resources>